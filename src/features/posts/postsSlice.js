import {createSlice} from '@reduxjs/toolkit'

const initialState = {
    posts: []
}

export const PostsSlice = createSlice({
    name: 'posts',
    initialState,
    reducers: {
        
    }
})